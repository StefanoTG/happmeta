# Allow the bot to restart the API service without a password.
# Installed to /etc/sudoers.d/subproxy by install.sh
root ALL=(ALL) NOPASSWD: /bin/systemctl restart subproxy-api, /bin/systemctl reload subproxy-api
